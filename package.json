{
  "name": "quill",
  "private": true,
  "version": "1.0.0",
  "description": "Quill - a Medium-style microblog powered by React and Neon Postgres",
  "scripts": {
    "setup": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "db:init": "node server/scripts/db-init.mjs",
    "db:seed": "node server/scripts/db-seed.mjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
